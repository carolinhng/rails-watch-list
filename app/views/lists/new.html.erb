<div class="container margin-top margin-bottom padding-top-10">
  <h1>Add a new list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit class: "btn-ghost border-radius-0"%>
  <% end %>


</div>
